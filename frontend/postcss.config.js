// PostCSS configuration — required by Tailwind CSS
export default {
  plugins: {
    tailwindcss: {},   // Process Tailwind directives (@tailwind, @apply, etc.)
    autoprefixer: {},  // Add vendor prefixes for cross-browser compatibility
  },
};
